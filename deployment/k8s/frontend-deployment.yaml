apiVersion: apps/v1
kind: Deployment
metadata:
  name: sical-frontend
  namespace: sical
  labels:
    app: sical-frontend
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sical-frontend
  template:
    metadata:
      labels:
        app: sical-frontend
        version: v1
    spec:
      containers:
      - name: frontend
        image: sical-frontend:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 80
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        env:
        - name: NODE_ENV
          value: "production"
---
apiVersion: v1
kind: Service
metadata:
  name: sical-frontend-service
  namespace: sical
  labels:
    app: sical-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: sical-frontend
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sical-frontend-ingress
  namespace: sical
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
  - host: sical.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: sical-frontend-service
            port:
              number: 80